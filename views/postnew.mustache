<!DOCTYPE html>
<html>
  {{>_head}}
  <body>
      <div class="row container main">

         {{#beerpost}}
         <div class="post">
          <img class="pic" src={{url}} alt="{{name}}"/>
         <a href={{beer}}>{{name}}</a>
        <p>{{type}} - {{brewery}}</p>

        </div>
       {{/beerpost}}

    </div>
    {{>_footer}}
  </body>
</html>
